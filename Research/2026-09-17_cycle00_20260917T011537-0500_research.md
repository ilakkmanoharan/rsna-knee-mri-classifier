# Research write-up — competition day 2026-09-17, cycle 00_20260917T011537-0500

_Generated 2026-09-17 06:20 UTC by agent1._

## Goal

Improve macro ROC-AUC on RSNA Knee Abnormality Detection (12 independent study-level targets) beyond the Stage-B prevalence baseline (~0.494 public).

## Methods to consider

### 1. Plane-aware multi-series aggregation

**Why it should help:** Sagittal/coronal/axial emphasize different structures (ACL sagittal, MCL coronal, PF OA axial). Pooling all series together dilutes plane-specific signal and hurts macro AUC equally across 12 targets.

**How to apply:** Encode slices → series emb → fixed plane slots + availability mask; never treat missing plane as clinical negative.

### 2. Weak supervision from multilingual reports

**Why it should help:** Only ~58 gold labels vs thousands of reports. Ignoring reports leaves ranking models undertrained; hard pseudo-labels from negation mistakes invert targets.

**How to apply:** Negation/uncertainty/temporality-aware parser → soft labels + confidence weights; calibrate on gold folds only; never use test reports.

### 3. Small-n gold-set metadata logistic

**Why it should help:** Hand-tuned plane/fluid offsets only moved public macro AUC 0.494→0.499. The 58 gold labels fitted a 7-d ridge logistic that reached 0.514. That is now the frozen floor, not a research idea.

**How to apply:** Keep gold_meta_logit as fallback. Do not resubmit it as the day's first cycle; ablate ranking changes on top of it.

### 4. Plane × protocol interaction ranks

**Why it should help:** Additive sag/cor/ax fractions plus a global fluid fraction cannot represent 'sagittal fluid-sensitive' (ACL/meniscus/effusion) vs 'axial fluid-sensitive' (PF OA, Baker's). Public models (MRNet plane-wise logits; RSNA knee label-attention with plane×fluid×fat counts) treat those combinations as first-class features. A 13-d interaction logit on the same 58 gold rows can re-rank fluid/OA heads.

**How to apply:** Extend the 7-d vector with sag/cor/ax × fluid and sag/cor/ax × fat fractions; fit ridge λ=3.5; rank-blend 0.60·7-d + 0.40·interact so a noisy interact head cannot fully overwrite the 0.514 ranking.

### 5. Per-target calibrated prevalence prior blend

**Why it should help:** Macro AUC cares about ranking per target. Pure constant prevalence (~0.49 public) is a floor; blending study-level model logits with target prevalence stabilizes rare labels (MCL, Baker's).

**How to apply:** p = clip((1-α)·σ(logit) + α·prev_t); tune α on OOF; larger α for rare/unstable heads.

### 6. Fluid-sensitive / fat-suppression series routing

**Why it should help:** train_series.csv exposes Fluid_Sensitive and Fat_Suppression. Contusion/effusion/synovitis benefit from fluid-sensitive series; bone findings may prefer others.

**How to apply:** Weight series embeddings by sequence flags before plane pooling; ablate via ASRA H-routing.

### 7. Frozen encoder then last-block unfreeze

**Why it should help:** Tiny gold set overfits if the full backbone trains early. Frozen ImageNet/RadImageNet encoder + heads first is the reliable baseline.

**How to apply:** Train heads only until OOF beats prevalence; then unfreeze last block at low LR.

### 8. Target-specific view preference soft regularizer

**Why it should help:** Hard rules (no sagittal ⇒ ACL=0) destroy recall. Soft preferred-plane coverage features improve ranking without forcing zeros.

**How to apply:** Concatenate preferred-plane coverage; small consistency loss only after ablation accepts it.

### 9. Ensemble fold checkpoints + rank average

**Why it should help:** Single fold on 58 labels is noisy. Probability or rank ensembling across folds lifts macro AUC more than architecture churn.

**How to apply:** Save fold*.pt; at inference mean probs or average ranks then rescale to [eps,1-eps].

## Priority for next submission (research-driven)

1. **Rank-blend plane×protocol interactions** on top of the frozen 7-d gold_meta_logit (0.514) — do not replace learned ranks with constants (report-shrinkage cannot change AUC).
2. Keep **gold_meta_logit** as the fallback notebook if the blend does not beat 0.514.
3. Add **report weak-supervision** only on train; inference must stay MRI/metadata-only.
4. Only then spend quota on heavier visual encoder changes (plane-aware EfficientNet / label-attention) once metadata ablations stall.

## Literature / arXiv notes

arXiv Atom API returned HTTP 406 from this environment. Methods below are from the
competition data page, MRNet, and public RSNA-knee write-ups retrieved this cycle.

- **Bien et al., MRNet (PLOS Medicine 2018)** — three plane-specific networks (sagittal T2,
  coronal T1, axial PD) with a *per-task logistic* that fuses plane logits. Direct analogue:
  do not collapse planes into one fraction; keep plane×protocol combinations separable.
  https://journals.plos.org/plosmedicine/article?id=10.1371/journal.pmed.1002699
- **RSNA competition data** — `train_series.csv` / `test_series.csv` expose only
  `Anatomical_Plane`, `Fluid_Sensitive`, `Fat_Suppression`. Test reports are absent. Any
  ranking model that stays offline must live in that 3-flag product space until pixels are
  trained on real DICOMs.
  https://www.kaggle.com/competitions/rsna-knee-abnormality-detection/data
- **Public RSNA-knee label-attention model (Hugging Face `leminhhung0101/knee-model`)** —
  study-level metadata includes counts of plane×fluid×fat combinations (e.g. Sagittal +
  Fluid + Fat) and a pairwise ranking loss because the metric is macro AUC.
- **UnifiedTheory / rank averaging** — competition metric is order-only; rank-average
  complementary heads rather than averaging raw probabilities. This cycle’s 0.60/0.40
  blend follows that rule: keep the accepted 7-d ranks as the majority vote.
- **Devpost “Reading the Report, Then the Knee”** — 58 gold labels vs ~4400 reports;
  test.csv has no Report. Weak labels are train-only. Do not open test radiology reports.

This cycle tests item 4 (plane×protocol interaction ranks) on top of the frozen 0.514 model.

## Fetch errors

- knee MRI multi-label abnormality detection deep learning: RuntimeError: HTTP Error 406: Not Acceptable
- RSNA knee abnormality detection: RuntimeError: HTTP Error 406: Not Acceptable
- multi-planar MRI aggregation CNN EfficientNet: RuntimeError: HTTP Error 406: Not Acceptable
- radiology report weak supervision negation: RuntimeError: HTTP Error 406: Not Acceptable
- macro ROC-AUC multi-label medical imaging: RuntimeError: HTTP Error 406: Not Acceptable
- plane-aware MRI classification sagittal coronal axial: RuntimeError: HTTP Error 406: Not Acceptable
