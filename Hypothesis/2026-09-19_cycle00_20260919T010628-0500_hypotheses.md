# Hypothesis analysis — competition day 2026-09-19, cycle 00_20260919T010628-0500

_Generated 2026-09-19 06:11 UTC by agent1._

Inputs: `2026-09-19_cycle00_20260919T010628-0500_research.md`, `2026-09-19_cycle00_20260919T010628-0500_analysis.md`

## Why this hypothesis now

gold_rank_w50 (0.50·7-d + 0.50·13-d plane×protocol) is the frozen public baseline at
**0.518** (submission 56322623). gold_rank_interact (0.60/0.40) scored 0.517; 7-d alone
scored 0.514; hand-tuned offsets saturated at 0.498–0.505. Per-target constant shrinkage
cannot change ROC-AUC. The next testable change is a **blend-weight ablation in the
other direction**: 0.70/0.30 (more 7-d) instead of resubmitting 0.50/0.50. Visual MRI
encoders stay out of scope until this metadata ablation beats 0.518 or is clearly falsified.

## Primary hypothesis this cycle

- **ID:** `H_gold_rank_w70`
- **Hypothesis:** Putting more weight on the proven 7-d ranks (0.70/0.30) will beat 0.518 if the 13-d interact head is noisy on rare targets at equal weight.
- **Mechanism:** Same two learned heads as gold_rank_w50; blend 0.70·rank(7-d)+0.30·rank(interact). Fallback to 7-d if interact fit fails. No test reports, no pixels, no Gaussian noise.
- **Falsification:** Public score ≤ 0.518 (frozen gold_rank_w50).
- **Expected targets:** ACL, MCL, Medial Meniscus, Lateral Meniscus

## Test protocol

- One offline Kaggle notebook; internet disabled; discover `sample_submission.csv`.
- Fit only on gold rows of `train.csv` joined to `train_series.csv`.
- Infer from `test_series.csv` metadata only — never open test radiology reports.
- Accept into the frozen baseline iff public score > 0.518 and status COMPLETE.
- Otherwise keep `gold_rank_w50` (0.518) as the fallback path.

## Backlog (ASRA queue)

- `H_gold_rank_w70` ← primary: Putting more weight on the proven 7-d ranks (0.70/0.30) will beat 0.518 if the 13-d interact head is noisy on rare targets at equal weight.
- `H_gold_rank_w50` : Equal rank-blend (0.50·7-d + 0.50·13-d interact) of the frozen gold_rank_interact heads will lift public macro ROC-AUC above 0.517 because the interaction head already added +0.003 at 0.40 weight.
- `H_gold_rank_interact` : Rank-blending the accepted 7-d gold_meta_logit ranks (public 0.514) with a stronger-regularized plane×fluid / plane×fat interaction logit will lift macro ROC-AUC above 0.514 without replacing the frozen ranking.
- `H_gold_meta_logit` : Ridge logistic models fit on the 58 gold-labeled train studies using series-metadata features will rank test studies better than hand-tuned plane/fluid offsets (public macro AUC > 0.499).
- `H_ensemble_rank` : Rank-average of prevalence and metadata models beats any single model.
- `H_report_shrinkage` : Train-time report soft-label shrinkage priors (global target means conditioned on evidence state frequencies) improve calibration of the metadata model without using test reports.
- `H_plane_slots_visual` : Frozen EfficientNet-B0 with plane slots trained on real (or JPEG) MRI lifts macro AUC over metadata-only.

## Decision rule

Accept into the next frozen baseline only if public score improves and no unexplained single-target collapse. Otherwise keep previous best notebook as the fallback submission path.

## Outcome

Public score **0.516** (submission 56350384, COMPLETE). `H_gold_rank_w70` is **falsified** (≤ 0.518). Frozen fallback remains `gold_rank_w50`. Interact weight has been monotonically helpful (0.00→0.514, 0.30→0.516, 0.40→0.517, 0.50→0.518). Next cycle should test `H_gold_rank_w40` (0.40/0.60), not resubmit 0.70/0.30 or 0.50/0.50.
