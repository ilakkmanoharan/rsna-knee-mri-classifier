# Hypothesis analysis — competition day 2026-09-17, cycle 00_20260917T011537-0500

_Generated 2026-09-17 06:20 UTC by agent1._

Inputs: `2026-09-17_cycle00_20260917T011537-0500_research.md`, `2026-09-17_cycle00_20260917T011537-0500_analysis.md`

## Why this hypothesis now

gold_meta_logit (learned 7-d series metadata, no noise) is the frozen public baseline at 0.514.
Hand-tuned offsets + Gaussian jitter saturated at 0.498–0.499; replacing the learned ranks
with report-shrinkage priors dropped the score to 0.504. Per-target constant shrinkage cannot
change ROC-AUC. The next testable change must *re-rank* studies: blend plane×fluid and
plane×fat interaction logits on top of the accepted 7-d ranks (0.60 / 0.40). Visual MRI
encoders stay out of scope until this metadata ablation beats 0.514 or is clearly falsified.

## Primary hypothesis this cycle

- **ID:** `H_gold_rank_interact`
- **Hypothesis:** Rank-blending the accepted 7-d gold_meta_logit ranks (public 0.514) with a stronger-regularized plane×fluid / plane×fat interaction logit will lift macro ROC-AUC above 0.514 without replacing the frozen ranking.
- **Mechanism:** Fit the frozen 7-d ridge logistic (λ=2) and a 13-d interaction model (λ=3.5) on the 58 gold labels using only train_series.csv flags. Rank-transform each head, then 0.60·rank(7-d)+0.40·rank(interact). Map ranks through gold prevalence. No test reports, no DICOM pixels, no Gaussian noise.
- **Falsification:** Public score ≤ 0.514 (frozen gold_meta_logit) or a notebook error falls back to 7-d ranks only.
- **Expected targets:** Effusion, Synovitis, Contusion, ACL, Medial OA

## Test protocol

- One offline Kaggle notebook; internet disabled; discover `sample_submission.csv`.
- Fit only on gold rows of `train.csv` joined to `train_series.csv`.
- Infer from `test_series.csv` metadata only — never open test radiology reports.
- Accept into the frozen baseline iff public score > 0.514 and status COMPLETE.
- Otherwise keep `gold_meta_logit` (0.514) as the fallback path.

## Backlog (ASRA queue)

- `H_gold_rank_interact` ← primary: Rank-blending the accepted 7-d gold_meta_logit ranks (public 0.514) with a stronger-regularized plane×fluid / plane×fat interaction logit will lift macro ROC-AUC above 0.514 without replacing the frozen ranking.
- `H_gold_meta_logit` : Ridge logistic models fit on the 58 gold-labeled train studies using series-metadata features will rank test studies better than hand-tuned plane/fluid offsets (public macro AUC > 0.499).
- `H_meta_prior` : Series-metadata features (plane availability + fluid-sensitive counts) blended with per-target prevalence will beat pure prevalence on public macro AUC.
- `H_report_shrinkage` : Train-time report soft-label shrinkage priors (global target means conditioned on evidence state frequencies) improve calibration of the metadata model without using test reports.
- `H_plane_slots_visual` : Frozen EfficientNet-B0 with plane slots trained on real (or JPEG) MRI lifts macro AUC over metadata-only.
- `H_fluid_routing` : Up-weighting fluid-sensitive series for effusion/synovitis/contusion improves those target AUCs without hurting ligaments.
- `H_ensemble_rank` : Rank-average of prevalence, metadata, and visual models beats any single model.

## Decision rule

Accept into the next frozen baseline only if public score improves and no unexplained single-target collapse. Otherwise keep previous best notebook as the fallback submission path.

## Cycle outcome

Public score **0.517** (ref `56296980`, COMPLETE) beat 0.514. **Accept** `gold_rank_interact`
as the new frozen baseline.
