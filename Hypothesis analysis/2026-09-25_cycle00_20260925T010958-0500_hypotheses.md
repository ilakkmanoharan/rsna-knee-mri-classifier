# Hypothesis analysis — competition day 2026-09-25, cycle 00_20260925T010958-0500

_Generated 2026-09-25 06:12 UTC by agent1._

Inputs: `2026-09-25_cycle00_20260925T010958-0500_research.md`, `2026-09-25_cycle00_20260925T010958-0500_analysis.md`

## Why this hypothesis now

gold_rank_w50 remains the frozen public baseline at **0.518**. Parser-only
`weak_rank_calibrate` scored **0.499** (56455239). `weak_rank_goldfill` scored **0.504**
(56484736). `weak_rank_confident` scored **0.511** (56513588) — best weak-label so far,
still below 0.518. Discussion 734117: named objects recover; graded/unstated fail.
The next testable change is **`weak_rank_named`**: keep gold 0/1 on the 58 and use
parser pos/neg only on ACL / Baker's / MCL. Visual MRI encoders stay out of scope.

## Primary hypothesis this cycle

- **ID:** `H_weak_rank_named`
- **Hypothesis:** Restricting parser pos/neg labels to named objects (ACL, Baker's, MCL) will beat 0.518, because all-target confident labels scored 0.511 and discussion 734117 says only named objects recover while graded/unstated fail.
- **Mechanism:** Same 7-d/13-d heads and 0.50/0.50 blend. Gold 0/1 when finite. Parser pos/neg only for ACL / Baker's / MCL; other unlabeled targets stay NaN. Never open test reports. Fallback to gold_rank_w50 if n_weak<20.
- **Falsification:** Public score ≤ 0.518 (frozen gold_rank_w50).
- **Expected targets:** ACL, Baker's, MCL

## Test protocol

- One offline Kaggle notebook; internet disabled; discover `sample_submission.csv`.
- Fit only on gold rows of `train.csv` joined to `train_series.csv`.
- Infer from `test_series.csv` metadata only — never open test radiology reports.
- Accept into the frozen baseline iff public score > 0.518 and status COMPLETE.
- Otherwise keep `gold_rank_w50` (0.518) as the fallback path.

## Backlog (ASRA queue)

- `H_weak_rank_named` ← primary: Restricting parser pos/neg labels to named objects (ACL, Baker's, MCL) will beat 0.518, because all-target confident labels scored 0.511 and discussion 734117 says only named objects recover while graded/unstated fail.
- `H_weak_rank_confident` : Keeping expert 0/1 on the 58 and using only parser pos/neg mentions (masking unmentioned/uncertain/historical) will beat 0.518, because goldfill's unmentioned=0.38 mass on ~4,349 reports scored 0.504 and empty extractions should stay unlabeled (discussion 734117).
- `H_weak_rank_goldfill` : Keeping expert 0/1 labels on the 58 gold studies and using parser soft labels only for unlabeled train reports will beat 0.518, because parser-only weak_rank_calibrate scored 0.499 after overwriting gold.
- `H_weak_rank_calibrate` : Fitting the frozen 7-d/13-d series-metadata heads on multilingual train-report soft labels (n≈4407) and using the 58 gold studies only to map ranks through prevalence will beat 0.518, because gold-only ranking has stalled (w50=0.518, w40=0.518, lam2=0.515) and series composition already reaches ~0.595 on report-derived labels.
- `H_gold_rank_lam2` : If the 0.40/0.60 and 0.50/0.50 blends both score 0.518, the 13-d interact head is over-regularized (λ=3.5) and nearly collinear with the 7-d ranks; lowering λI to 2.0 at the frozen 0.50 blend will make plane×protocol ranks distinctive and beat 0.518.
- `H_gold_rank_w40` : Giving the 13-d interact head majority weight (0.40/0.60) will beat 0.518 because public LB has risen monotonically as interact weight went 0.00→0.30→0.40→0.50.
- `H_gold_rank_w70` : Putting more weight on the proven 7-d ranks (0.70/0.30) will beat 0.518 if the 13-d interact head is noisy on rare targets at equal weight.
- `H_gold_rank_w50` : Equal rank-blend (0.50·7-d + 0.50·13-d interact) of the frozen gold_rank_interact heads will lift public macro ROC-AUC above 0.517 because the interaction head already added +0.003 at 0.40 weight.
- `H_gold_rank_interact` : Rank-blending the accepted 7-d gold_meta_logit ranks (public 0.514) with a stronger-regularized plane×fluid / plane×fat interaction logit will lift macro ROC-AUC above 0.514 without replacing the frozen ranking.
- `H_gold_meta_logit` : Ridge logistic models fit on the 58 gold-labeled train studies using series-metadata features will rank test studies better than hand-tuned plane/fluid offsets (public macro AUC > 0.499).
- `H_ensemble_rank` : Rank-average of prevalence and metadata models beats any single model.
- `H_report_shrinkage` : Train-time report soft-label shrinkage priors (global target means conditioned on evidence state frequencies) improve calibration of the metadata model without using test reports.
- `H_plane_slots_visual` : Frozen EfficientNet-B0 with plane slots trained on real (or JPEG) MRI lifts macro AUC over metadata-only.

## Decision rule

Accept into the next frozen baseline only if public score improves and no unexplained single-target collapse. Otherwise keep previous best notebook as the fallback submission path.

## Outcome of this cycle

Public score **0.502** (submission 56541791, COMPLETE). `H_weak_rank_named` is **falsified** (≤ 0.518) and is a **regression vs `weak_rank_confident` (0.511)**. Restricting parser labels to ACL / Baker's / MCL while fitting all 12 heads on the 4,407-row feature matrix likely shifted standardization for the non-named gold heads. Frozen fallback remains `gold_rank_w50`. Next cycle should test `H_weak_rank_named_mix` (keep gold_rank_w50 ranks for all 12; 50/50 mix named-weak ranks onto ACL / Baker's / MCL only). Do not resubmit `weak_rank_named`.
